const expected = `#include <cassert>
#include <ctime>
#include <functional>
#include <memory_resource>
pr-3
pr-32
sadasda
asdadad
asd
asd
asm(std::default_searcher<std::default_searcher<ADJ_ESTERRORas
    std::default_searcher<std::default_searcher<std::default_searcher<static_assert(stdd
        cdscdc
        std::pmr::set_default_resource(std::default_searcher<
            std::pmr::set_default_resource(std
            std
        ADJ_ESTERRORs
        cdsvsdvsd
        sdcds
        cscd
    ADJ_ESTERRORa

static_assert(stdas
    ADJ_ESTERROR
    assert(ADJ_ESTERRORs
        sdcsdc
    ADJ_ESTERRORa), );)>), );>>>>>)




    dcsdcs`;

console.log("Expected length:", expected.length);
